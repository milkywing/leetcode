#include <leet/priority_queue.h>

#include <iostream>

int main(int argc, char const *argv[]) {
  std::cout << "start leetcoding!" << std::endl;
  leet::PriorityQueue<int> pq({5, 3, 1, 2, 2, 4, -100});
  return 0;
}
